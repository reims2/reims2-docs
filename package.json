{
  "scripts": {
    "dev": "vitepress dev src",
    "build": "vitepress build src",
    "preview": "vitepress preview src",
    "start": "vitepress preview src --host $HOST --port $PORT"
  },
  "packageManager": "yarn@3.6.1",
  "license": "AGPL-3.0-only",
  "dependencies": {
    "vitepress": "1.0.0-beta.3"
  },
  "engines": {
    "node": "18.x"
  }
}
