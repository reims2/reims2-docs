{
  "scripts": {
    "dev": "vitepress dev src",
    "build": "vitepress build src",
    "preview": "vitepress preview src --base docs",
    "start": "vitepress preview src --port $PORT --base docs"
  },
  "packageManager": "yarn@3.6.1",
  "license": "AGPL-3.0-only",
  "dependencies": {
    "vitepress": "1.0.0-beta.5"
  },
  "engines": {
    "node": "18.x"
  }
}
